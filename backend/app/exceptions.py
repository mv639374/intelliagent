# This file will hold custom exception handlers.
# For example, you can add handlers to catch SQLAlchemy errors
# and return a 404 response.

# from fastapi import Request
# from fastapi.responses import JSONResponse
# from sqlalchemy.exc import NoResultFound

# async def sqlalchemy_not_found_exception_handler(request: Request, exc: NoResultFound):
#     return JSONResponse(
#         status_code=404,
#         content={"message": "The requested resource was not found."},
#     )
