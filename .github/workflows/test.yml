# .github/workflows/test.yml
name: Run Tests

on:
  workflow_call:

jobs:
  test-backend:
    name: Test Backend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: uv pip install -e ".[dev]"

      - name: Run Pytest
        run: uv run pytest

  test-frontend:
    name: Test Frontend (Placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Run frontend tests
        run: echo "🧪 No frontend tests configured yet."
